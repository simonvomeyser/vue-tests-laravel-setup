import Vue from 'vue'

// Vue.prototype.flash = () => {};
// Vue.prototype.trans = (string) => string;
// Vue.prototype.route = (string) => string;
// Vue.prototype.debounce = (callback) => {callback()};
//
// require('../../resources/js/setup/vue-plugins');
//
// jest.mock('../../resources/js/components/eventbus')
